<Window x:Class="R6Planner.Views.OperatorIconDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Operator Icon" Height="600" Width="500"
        WindowStyle="None" AllowsTransparency="False"
        Background="#0D1117" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="DarkBtn" TargetType="Button">
            <Setter Property="Background" Value="#1C2230"/>
            <Setter Property="Foreground" Value="#C9D1D9"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2D3748"/>
                                <Setter Property="BorderBrush" Value="#58A6FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkScrollBar" TargetType="ScrollBar">
            <Setter Property="Background" Value="#0D1117"/>
            <Setter Property="Foreground" Value="#30363D"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="6"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Setter Property="Background" Value="#30363D"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="6" Margin="2"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#58A6FF"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <Grid Grid.Row="0" Background="#010409" MouseLeftButtonDown="TitleBar_Drag">
            <TextBlock Text="SELECT OPERATOR ICON" Foreground="#C9D1D9"
                       FontFamily="Consolas" FontSize="13" FontWeight="Bold"
                       VerticalAlignment="Center" Margin="12,0"/>
            <Button Style="{StaticResource DarkBtn}" Content="✕" Width="32"
                    Click="CloseBtn_Click" Padding="0"
                    HorizontalAlignment="Right" Margin="0,0,4,0"
                    Background="#3B1A1A"/>
        </Grid>

        <!-- Search box -->
        <Border Grid.Row="1" Margin="12,12,12,0" Background="#161B22"
                BorderBrush="#21262D" BorderThickness="1" CornerRadius="3">
            <TextBox x:Name="TbSearch" Background="Transparent" Foreground="#C9D1D9"
                     BorderThickness="0" FontFamily="Consolas" FontSize="11"
                     Padding="8,6" CaretBrush="White"
                     TextChanged="TbSearch_TextChanged">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Grid>
                                        <TextBlock Text="🔍 Search operators..."
                                                   Foreground="#555F6E"
                                                   FontFamily="Consolas" FontSize="11"
                                                   Padding="8,6"
                                                   IsHitTestVisible="False">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <ScrollViewer x:Name="PART_ContentHost"/>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Border>

        <!-- Operator grid -->
        <Border Grid.Row="2" Margin="12,8,12,0" Background="#161B22"
                BorderBrush="#21262D" BorderThickness="1" CornerRadius="3">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource DarkScrollBar}"/>
                </ScrollViewer.Resources>
                <WrapPanel x:Name="OperatorGrid" Margin="8"/>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <UniformGrid Grid.Row="3" Columns="3" Margin="12" Height="32">
            <Button Style="{StaticResource DarkBtn}" Content="None (A/D)" Margin="0,0,4,0"
                    Click="NoneBtn_Click"/>
            <Button Style="{StaticResource DarkBtn}" Content="Select" Margin="2,0"
                    Click="SelectBtn_Click" IsDefault="True"/>
            <Button Style="{StaticResource DarkBtn}" Content="Cancel" Margin="4,0,0,0"
                    Click="CloseBtn_Click" IsCancel="True"/>
        </UniformGrid>
    </Grid>
</Window>
